# Example only: replace the service name with the gateway address in your deployment.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: inference-gateway
    metrics_path: /metrics
    static_configs:
      - targets:
          - gateway:8080
